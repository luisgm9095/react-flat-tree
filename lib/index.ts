import { addToArray, exampleData } from 'immutable-flat-tree';

export const addSomeValue = () => addToArray('fixed value');
export const myExampleData = () => exampleData

export { Example } from './components/example';
